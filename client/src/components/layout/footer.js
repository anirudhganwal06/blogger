import React from 'react';

const footer = props => {
    return (
        <footer className="bg-dark text-center">
            Copyright &copy; {new Date().getFullYear()} Blogger
        </footer>
    );
}

export default footer;
